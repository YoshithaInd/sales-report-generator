# sales-report-generator
Build a command-line tool that reads a CSV file containing daily product sales  data, computes a summary report, and outputs the result either to the console or saves it to a text  file, based on what the user specifies as a command-line argument.
